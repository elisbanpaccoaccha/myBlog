---
import Layout from "../../../layouts/Layout.astro";
import { ArrowRight, ThumbsUp, MessageSquare, Slash } from "@lucide/astro";

import DetallesAutor from "../../../components/ui/DetallesAutor.astro";
import Quote from "../../../components/ui/Quote.astro";
import ReadCode from "../../../components/ui/ReadCode.astro";

const titulo: string = "Building Design System with Tailwind CSS v4 and Astro.";
---

<Layout
  title="Blog Tech - Hello World"
  ,
  description="Building Design System with Tailwind CSS v4 and Astro."
>
  <section
    class="relative z-10 w-full max-w-4xl mx-auto px-4 py-12 md:py-16 text-gray-200 dark:text-gray-800"
  >
    <div
      class="bg-white dark:bg-gray-900 rounded-xl shadow-lg border border-gray-200 dark:border-gray-800 overflow-hidden"
    >
      <div
        class="bg-gray-50 dark:bg-gray-800 border-b border-gray-100 dark:border-gray-700 px-4 py-3 flex items-center gap-4"
      >
        <div class="flex gap-2">
          <div
            class="w-3 h-3 rounded-full bg-[#FF5F56] border border-[#E0443E]"
          >
          </div>
          <div
            class="w-3 h-3 rounded-full bg-[#FFBD2E] border border-[#DEA123]"
          >
          </div>
          <div
            class="w-3 h-3 rounded-full bg-[#27C93F] border border-[#1AAB29]"
          >
          </div>
        </div>
        <div class="font-mono text-xs text-gray-400 select-none">
          {titulo.toLowerCase().replace(/\s+/g, "-")}
        </div>
      </div>

      <div class="p-8 md:p-16">
        <DetallesAutor
          fecha={"2023-11-14"}
          autor={"Dev Coder"}
          tags={["css", "architecture"]}
        />

        <header class="mb-12">
          <h1
            class="font-mono font-bold text-4xl md:text-5xl lg:text-6xl text-gray-900 dark:text-gray-50 tracking-tight leading-tight"
          >
            <span class="text-[#A755F7]">{`{`}</span>
            Building a <br class="hidden md:block" />
            Design System
            <span class="text-[#A755F7]">{`}`}</span>
          </h1>

          <div
            class="mt-4 font-mono text-gray-500 dark:text-gray-400 text-sm md:text-base pl-2 border-l-2 border-brand-pink/50"
          >
            <span class="text-brand-pink font-bold">01</span>
            Scaling a frontend application requires more than just good code.
          </div>
        </header>

        <article
          class="prose prose-slate prose-lg max-w-none prose-headings:font-mono prose-p:font-sans"
        >
          <p
            class="lead text-xl text-gray-600-dark dark:text-gray-400 mb-8 font-light"
          >
            Scaling a frontend application requires more than just good code; it
            requires a consistent visual language. In this post, we'll refactor
            our color palette variables for better scalability.
          </p>

          <p>
            When we started this project, we hardcoded hex values everywhere.
            <code class="text-sm">#3b82f6</code> was scattered across 50 files. As
            the team grew, so did the inconsistencies.
          </p>

          <h2 class="text-2xl font-bold mt-16 mb-6 font-mono text-gray-400">
            <span class="text-[#94A2B8] font-bold size-5">{`##`}</span>
            The Problem
          </h2>

          <p>
            Imagine trying to change your primary brand color. You do a global
            find and replace, and suddenly your validation errors (red) are now
            your brand color. We need a layer of abstraction.
          </p>

          <ReadCode
            codigo={`
.button-primary {
  background-color: #3b82f6;
  color: #ffffff;
}

.alert-error {
  color: #ef4444;
}
`}
            lenguaje={"css"}
            nombreFile={"styles.css"}
          />

          <h2 class="text-2xl font-bold mt-16 mb-6 font-mono text-gray-400">
            <span class="text-[#94A2B8] font-bold size-5">{`##`}</span>
            Tokenizing Colors
          </h2>

          <p>
            Design tokens are the visual atoms of the design system. Instead of
            using raw hex values, we define tokens that describe the <em
              >intent</em
            > of the color.
          </p>

          <ReadCode
            nombreFile="tokens.json"
            codigo=`{
  "colors": {
    "primary": {
      "main": "#3b82f6",
      "hover": "#2563eb"
    },
    "semantic": {
      "success": "#10b981",
      "error": "#ef4444"
    }
  }
}`
            lenguaje="json"
          />

          <Quote author="Nathan Curtis">
            A design system isn't a project, it's a product serving products.
          </Quote>

          <p>
            By using these tokens, we can now update the entire theme by
            changing a single file. This is especially powerful when
            implementing dark mode.
          </p>

          <div class="mt-12 pt-8 border-t border-dashed border-gray-200">
            <h3
              class="text-xl font-bold font-mono mb-4 text-gray-900 dark:text-gray-50 flex items-center gap-2"
            >
              <span class="text-[#EB4999]">function</span> conclusion()
            </h3>
            <p>
              Start small. You don't need a massive system from day one. Begin
              by extracting your colors and typography into variables.
            </p>
          </div>
        </article>

        <div
          class="mt-16 pt-8 border-t border-gray-100 font-mono text-sm text-gray-500 flex flex-wrap gap-8 items-center"
        >
          <span
            class="flex items-center gap-2 hover:text-brand-pink cursor-pointer transition-colors group"
          >
            <span
              class="material-symbols-outlined text-sm group-hover:scale-110 transition-transform"
              ><ThumbsUp /></span
            >
            42 Likes
          </span>

          <span
            class="flex items-center gap-2 hover:text-brand-pink cursor-pointer transition-colors group"
          >
            <span
              class="material-symbols-outlined text-sm group-hover:scale-110 transition-transform"
              ><MessageSquare /></span
            >
            8 Comments
          </span>

          <span
            class="flex items-center gap-2 hover:text-brand-pink cursor-pointer transition-colors ml-auto text-brand-pink font-semibold group"
          >
            read_next()
            <span
              class="material-symbols-outlined text-sm group-hover:translate-x-1 transition-transform"
              ><ArrowRight /></span
            >
          </span>
        </div>
      </div>
    </div>
  </section>
</Layout>
